# Big Sur Glow — GTK 2, variante clara
#
# GTK 2 nao entende @define-color nem a sintaxe do GTK 3: e outra linguagem,
# por estados (NORMAL, PRELIGHT, ACTIVE, SELECTED, INSENSITIVE). Por isso
# este arquivo repete os hexadecimais em vez de referenciar a paleta.
# Se mudar shared/palette.json, mude aqui tambem.
#
# Sem engine: so cores. Um GIMP ou Inkscape nao vai ficar com widget de
# macOS, mas para de destoar do resto da area de trabalho — que e o ponto.
#
# Autor: MarbleCeo

gtk-color-scheme = "fg_color:#221F26\nbg_color:#ECEBF0\nbase_color:#FFFFFF\ntext_color:#221F26\nselected_bg_color:#9b87f5\nselected_fg_color:#FFFFFF\ntooltip_bg_color:#302D38\ntooltip_fg_color:#F4F2F8"

gtk-auto-mnemonics = 1
gtk-primary-button-warps-slider = 1
gtk-button-images = 0
gtk-menu-images = 0

style "bsg-default" {
  xthickness = 1
  ythickness = 1

  fg[NORMAL]       = @fg_color
  fg[PRELIGHT]     = @fg_color
  fg[ACTIVE]       = @fg_color
  fg[SELECTED]     = @selected_fg_color
  fg[INSENSITIVE]  = "#8A8794"

  bg[NORMAL]       = @bg_color
  bg[PRELIGHT]     = "#F5F4F8"
  bg[ACTIVE]       = "#E2E0E8"
  bg[SELECTED]     = @selected_bg_color
  bg[INSENSITIVE]  = @bg_color

  base[NORMAL]      = @base_color
  base[PRELIGHT]    = @selected_bg_color
  base[ACTIVE]      = "#C9BEF0"
  base[SELECTED]    = @selected_bg_color
  base[INSENSITIVE] = @bg_color

  text[NORMAL]      = @text_color
  text[PRELIGHT]    = @text_color
  text[ACTIVE]      = @selected_fg_color
  text[SELECTED]    = @selected_fg_color
  text[INSENSITIVE] = "#8A8794"

  GtkButton::child-displacement-x = 0
  GtkButton::child-displacement-y = 0
  GtkButton::default-border       = { 0, 0, 0, 0 }
  GtkButton::inner-border         = { 4, 4, 2, 2 }
  GtkCheckButton::indicator-size  = 15
  GtkRange::slider-width          = 14
  GtkRange::trough-border         = 2
  GtkRange::stepper-size          = 0
  GtkScrollbar::has-backward-stepper = 0
  GtkScrollbar::has-forward-stepper  = 0
  GtkScrollbar::min-slider-length    = 32
  GtkScrolledWindow::scrollbar-spacing = 0
  GtkPaned::handle-size           = 1
  GtkTreeView::vertical-separator = 2
  GtkTreeView::odd-row-color      = @base_color
  GtkTreeView::even-row-color     = "#FAF9FC"
  GtkMenuBar::internal-padding    = 2
  GtkExpander::expander-size      = 12
  GtkToolbar::internal-padding    = 2
}

style "bsg-menu" {
  bg[NORMAL] = "#F7F6FA"
  xthickness = 0
  ythickness = 0
}

style "bsg-menu-item" {
  fg[PRELIGHT] = @selected_fg_color
  bg[PRELIGHT] = @selected_bg_color
}

style "bsg-tooltip" {
  bg[NORMAL] = @tooltip_bg_color
  fg[NORMAL] = @tooltip_fg_color
  xthickness = 4
  ythickness = 4
}

style "bsg-entry" {
  bg[NORMAL]  = @base_color
  base[NORMAL] = @base_color
  xthickness = 4
  ythickness = 4
}

class "GtkWidget"    style "bsg-default"
class "GtkEntry"     style "bsg-entry"
class "GtkMenu"      style "bsg-menu"
class "GtkMenuItem"  style "bsg-menu-item"
widget "gtk-tooltip*" style "bsg-tooltip"
